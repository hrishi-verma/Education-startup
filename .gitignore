/node_modules
/.next
/out
/build
next-env.d.ts
.env*
.DS_Store
*.tsbuildinfo

# Graphify codebase knowledge graph — regenerable locally with `graphify update .`
# (kept out of git to avoid churn; rebuild on clone). See docs/frontend/architecture.md.
/graphify-out
